{
  "name": "firebase_rules_tests",
  "version": "1.0.0",
  "description": "Shoe Store Marketplace Security Rules Unit Tests",
  "main": "index.js",
  "scripts": {
    "test": "firebase emulators:exec --project test-finalprm-project \"mocha --timeout 10000 *.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.2",
    "mocha": "^10.2.0",
    "chai": "^4.3.7",
    "firebase-tools": "^13.0.0"
  }
}
